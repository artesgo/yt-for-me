// string, boolean, number, 

// example interface for writing instruments
export interface IWritable {
    color: string;
    eraser: boolean;
    erasable: boolean;
    brand: string;
    lineWidth: number; // in mm
}